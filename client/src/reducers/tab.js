// const tabReducer = (tab = "create", action) => {
//   switch (action.type) {
//     case "INIT_TAB":
//       return action.payload;
//     default:
//       return tab;
//   }
// };

// export default tabReducer;

// // dispatch({type:"INIT_TAB", payload: "profile"});
